// Separando contextos:

// Contexto do cálculo da área.

//Essa três linhas representam o contexto do cálculo da área pois todas elas são utilizadas no cálculo da área do triângulo.
let base = 16;
let altura = 8;

let area = (base * altura) / 2;

// contexto de exibição.

//O único objetivo dessa linha de código é exibir o resultado final, por isso ela fica no contexto de exibição.
console.log("A área do triangulo é "+area);